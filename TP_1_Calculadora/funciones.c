void suma(double x, double y);
/** \brief Mediante los dos operandos ingresados por usuario, se calcula la suma entre ambos.
 *
 * \param x: el primer operando ingresado por el usuario.
 * \param y: el segundo operando ingresado por el usuario.
 * \return No retorna valor, muestra el resultado en la misma función.
 *
 */

void resta(double x, double y);
/** \brief Mediante los dos operandos ingresados por usuario, se calcula la diferencia entre ambos.
 *
 * \param x: el primer operando ingresado por el usuario.
 * \param y: el segundo operando ingresado por el usuario.
 * \return No retorna valor, muestra el resultado en la misma función.
 *
 */

void division(double x, double y);
/** \brief Mediante los dos operandos ingresados por usuario, se calcula la division entre ambos.
 *
 * \param x: el primer operando ingresado por el usuario.
 * \param y: el segundo operando ingresado por el usuario.
 * \return No retorna valor, muestra el resultado en la misma función.
 *
 */


void multiplicacion(double x, double y);
/** \brief Mediante los dos operandos ingresados por usuario, se calcula el producto entre ambos.
 *
 * \param x: el primer operando ingresado por el usuario.
 * \param y: el segundo operando ingresado por el usuario.
 * \return No retorna valor, muestra el resultado en la misma función.
 *
 */


double factorial(double x);
/** \brief Se calcula el factorial de un numero entero.
 *
 * \param x: el primer operando ingresado por el usuario
 * \return retorna el valor de la operacion.
 *
 */

